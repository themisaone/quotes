# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=quotes_db
DB_USER=your_username
DB_PASSWORD=your_password

# Server Configuration
PORT=4000

# Note: File storage threshold is now configured in Settings UI (Storage & Performance)
# No need to set MAX_DB_SIZE_MB here - change it in the app!
